# Daemon JVM Criteria (Gradle 8.7+).
# Le pide a Gradle que arranque su propio Daemon con JDK 17, descargándolo
# vía foojay-resolver-convention si no está disponible localmente.
# Sin esto, plugins como SQLDelight 2.0.2 fallan al configurar.
toolchainVersion=17
